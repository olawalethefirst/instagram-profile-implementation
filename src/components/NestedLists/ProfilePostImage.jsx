import { View, Text } from 'react-native'
import React from 'react'

export default function ProfilePostImage({uri}) {
  return (
    <Image
        style={{ width, height: width, backgroundColor: color2b2b2b }}
        source={{ uri }}
      />
  )
}